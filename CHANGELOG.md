# 更新日志

每次 Git 提交都同步记录实际变更、验证结果与已知限制。

## [0.1.2] - 2026-09-09

### 修复

- 修复新版 Chromium 声明支持原生 HLS 后，控制页绕过 hls.js、在真实 B 站混流分片上解析失败并停播的问题。支持 MSE 的非 Apple 浏览器优先使用包内 hls.js，Apple 浏览器保留原生播放路径。
- 浏览器回归覆盖声明原生 HLS 能力的 Chromium，验证实际播放时间前进、偏移后恢复及本地 Blob Worker；更新版本号、使用说明和实播记录。

### 验证

- B 站 视频样本 A 与音频样本 B实播通过约 85 秒和 182 秒的功能流程，全程关闭自动 OCR。验证六档微调、复制地址、媒体 Range、普通退出恢复、恢复期间手动优先、停止与保存偏移后重连。
- 同期输入输出匹配 6 个像素相同的视频关键帧、675 个相同 AAC 包；源 PTS 相对位移误差约 0.167ms，DTS 异常为 0。偏移调整后约 4.2–6.2 秒恢复，主动普通退出后约 6.1 秒恢复。
- 排除真实 OCR 集成后的 109 项回归全部通过、0 跳过，耗时 67.21 秒；Ruff、JavaScript 语法和差异检查通过。源码包与 wheel 构建成功，核对播放器代码、hls.js、许可和版本完整性。
- 详细条件与测量含义见 [0.1.2 验证记录](docs/validation-0.1.2.md)。

### 已知限制

- 两个房间内容不同，视频样本 A当时播放赛事重播；本轮不验收同场比赛内容同步精度或 OCR。不同编码/线路 PTS 起点不同，切换后须重新确认偏移。
- 真实 B 站页面遇到专题跳转和验证码，页面嗅探仍待实播验收；视频直链及 B 站房间解析已通过。
- 本机 Chromium 不支持 HEVC，实播视频使用现有 H.264 线路并保持复制输出。未完成实体手机 Safari、Windows/macOS 原生、签名自然过期、实际长延迟缓冲及两小时连续运行验收。

## [0.1.1] - 2026-09-09

### 修复与功能

- 补齐“比赛源直连”设置：WebUI、CLI 和 P0 支持，覆盖页面嗅探、ffprobe、PyAV 采样及 FFmpeg 混流；B 站保留独立的代理访问。
- 修复空代理选项在 HLS 子请求中丢失的问题，使分片和 AES-128 密钥请求也遵守直连设置，保留请求上下文、源 PTS 和视频复制输出。
- 修复环境检查误拒绝新版 `N-…` FFmpeg 构建的问题，按运行时 libavformat 确认 FFmpeg 6 的兼容下限。
- FFmpeg 原生崩溃后停止自动重取源，显示更换构建的诊断并保留控制台重新连接能力；普通取流失败仍可恢复，旧测量不能覆盖手动偏移。ffprobe 崩溃也给出明确诊断。

### 验证

- 118 项完整回归通过，0 跳过：98 项基础/HTTP、9 项 FFmpeg 媒体、9 项真实 Tesseract、2 项浏览器流程；耗时 68.63 秒。
- 本地代理验证普通和 AES-128 HLS 的探测、采样、混流、分片及密钥直连，B 站请求继续经过代理；源 PTS 及视频像素保持验证通过。
- 同一本地合成 HLS 在此前使用的 7.0.2 静态构建上复现 SIGSEGV，在新构建上正常读取；新构建的 CLI 环境检查、Ruff 及 JavaScript 语法检查通过。
- 本轮环境与范围见 [0.1.1 验证记录](docs/validation-0.1.1.md)。

### 已知限制

- 此前两个实播测试地址已失效，本轮使用本地合成源，未完成新的实播验收。
- 真实比赛连续 2 小时、实际长延迟缓冲、实体手机 Safari 和 Windows/macOS 原生运行仍待验证；模拟 Windows 异常退出码不代表实机验收。

## [0.1.0] - 2026-09-09

### 功能

- 建立 P0/P1/P2 可运行基线：B 站 FLV 解析、比赛页面浏览器嗅探、源 PTS 对齐、视频复制混流和局域网 HLS 服务。
- WebUI 支持输入两路地址、直链及请求头、启停任务、线路选择、HLS 预览、六档偏移微调、两路截图翻转和时钟区域框选。
- OCR 支持 RapidOCR/Tesseract、已保存区域复用、自动发现、连续走表校验、停表拒绝、周期复核和连续两次漂移确认。
- 支持断流恢复、B 站重解析、第三方重嗅探、状态持久化与独立的 P0 手工验收命令。
- hls.js 与许可文件随包提供，控制页无需外部 CDN；HEVC 使用 fMP4 和独立初始化文件。

### 稳定性

- 控制页在取流前可用；OCR 在后台运行，新手动设置和新源时间轴不会被旧测量覆盖。
- WebUI 继承命令行的手动模式、初始偏移和直链设置；停止请求不会在嗅探启动时被清除。
- 嗅探依据播放列表的实际分片请求判断活跃线路，探测失败后可继续选线，取流请求保留所需请求上下文。
- HLS 重启使用独立分片名称，保留 discontinuity，避免覆盖旧分片和 HEVC 初始化文件；HTTP 支持媒体 Range 请求。
- 修正合成直播夹具中时间戳为 0 的 FLV 编码头被误作媒体起点的问题。

### 验证与版本管理

- 完整回归 90 项通过，0 跳过：72 项基础/HTTP 检查、7 项 FFmpeg 媒体集成、9 项真实 Tesseract 检查、2 项浏览器流程验证。
- 1440px 桌面与 390px 手机尺寸通过播放、框选、手动偏移及停止流程；本地加速合成源中调整后约 4.1 秒恢复播放。
- Ruff、JavaScript 语法检查通过；生成源码包及 wheel，并核对运行代码、WebUI、hls.js 与许可文件完整性。
- 新增 Git 忽略规则、协作约定和提交前检查钩子；每次提交必须同时更新并暂存本日志。
- 详细环境与验收范围见 [验证记录](docs/validation.md)。

### 已知限制

- 尚未完成真实比赛连续 2 小时、真实长延迟双路缓冲及实体手机 Safari 连播 30 分钟的验收。
- Windows/macOS 尚未实机验证；HEVC 播放取决于浏览器解码能力。
- 音频 GCC-PHAT 模块尚未接入控制流程，时钟跳变处逐帧精细化留待后续阶段。
