# 更新日志

每次 Git 提交都同步记录实际变更、验证结果与已知限制。

## [首次配置与一键启动] - 2026-09-17

- 新增 macOS/Linux 的 `setup.sh`、`start.sh` 和 Windows 的 `setup.bat`、`start.bat`，共用初始化逻辑。首次启动自动创建 `.venv`，安装项目、Tesseract Python 接口和 Playwright Chromium；支持单独初始化、指定 Python、外部程序路径及启动参数透传。
- 检查 FFmpeg 6.0+、ffprobe 和可选 Tesseract 英文数据；Linux 可通过 `setup.sh --with-deps` 安装 Chromium 系统库。后续启动复用环境，依赖声明变更、环境标记损坏、依赖或浏览器不可用时重新初始化；安装失败不保留成功标记。
- 固定项目工作目录，保留已有状态文件；使用无缓冲输出及时显示控制台链接，转交进程退出码并支持正常停止。补齐中英文说明、源代码包清单和 CI 脚本检查。
- 快速回归 **323 项通过**，包括 **16 项**初始化、重试、环境复用、参数及输出回归；Ruff、格式、Shell 语法和打包检查通过。Linux 临时全新环境完成依赖安装与初始化重试，重复配置保留状态，离线复用环境后控制台与状态接口可访问，`SIGINT` / `SIGTERM` 均正常退出。
- Chromium 下载受网络影响，实际浏览器验证使用复制到临时目录的同版本缓存；未实机验收 Windows/macOS。快速回归退出时出现既有的 Python 3.14 `HTTPResponse` 析构警告，不影响测试通过。

## [短 HLS 窗口卡顿修复] - 2026-09-14

- 远程 HLS 比赛源改由独立的 FFmpeg 输入进程持续读取，使用有上限的 FIFO 缓冲，避免双路混流等待较慢解说时，视频分片已从源播放列表过期而被跳过。画面与原声音轨继续复制，保留源 PTS、鉴权头和代理设置。
- 启动取消或混流进程启动失败时清理输入进程；停止、换线和调偏重启同时清理缓冲进程及诊断线程。
- 新增短窗口与延迟音频的真实媒体回归：同一场景旧实现仅保留 **225/400 帧**，修复后 **400/400 帧**，画面像素、音视频相对 PTS 和帧间连续性检查通过。相关单元与真实媒体回归共 **148 项通过**；Ruff、格式及差异检查通过。
- 现场独立双路对照消除了此前约 12 秒的视频时间戳缺口，留存分片的相邻帧间隔恢复为 0.04 秒。原会话的 OCR 后续已自行复核通过，本次没有修改 OCR 预算或识别算法。验证范围见 [记录](docs/validation/validation-hls-buffer-2026-09-14.md)；代码将在下次启动时加载。

## [中英文适配] - 2026-09-13

- 默认保留中文，新增 `README.en.md` 与英文第三方许可说明，中英文文档互链；更新 1440px / 390px 两种尺寸的中英文 Web UI 截图，展示未连接界面。
- Web UI 新增语言选择器并记住浏览器选择，覆盖静态说明、无障碍标签、播放器、OCR、候选线路和状态提示。切换语言保留输入、未保存的 ROI 和正在播放的会话。
- `footboy`、`footboy-p0` 与离线 OCR 诊断支持 `--lang en` / `--lang zh-CN` 和 `FOOTBOY_LANG`，补齐帮助、后台状态及错误信息。控制 API 按 `Accept-Language` 返回文案，不同客户端可以独立选择语言；源线路名称和 OCR 原文保持原样，诊断信息继续脱敏。
- 将语言资源纳入发行包，CI 增加语言脚本语法检查，提交钩子支持英文提示。完整回归 **374 项通过**，包括语言切换、跨客户端状态、浏览器存储受限、ROI 编辑保留和持续播放；Ruff、JavaScript / Shell 语法、文档本地链接和差异检查通过。

## [CI 双路 OCR 超时修复] - 2026-09-13

- 初始化 Tesseract 后端时默认设置 OMP_THREAD_LIMIT=1，减少双路识别与 OpenCV 线程池争用 CPU 导致的单次识别超时；保留用户显式设置，继续使用原有超时预算、取消机制和连续走表校验。
- 新增真实子进程环境继承测试，覆盖默认线程上限及用户覆盖；README 补充并发设置说明。
- 使用 GitHub Actions 相同的 Tesseract 5.3.4 / Leptonica 1.82.0，在四核限制下按完整 OCR 测试顺序复现 4 项双路识别失败。仅将 Tesseract 子进程线程上限设为 1 后，同一组 34 项测试全部通过。
- 修复后不额外设置线程环境变量，在同一旧版 OCR / 四核条件下 **34 项全部通过，23.75 秒**；快速回归 **286 项通过、2 项浏览器测试未启用**。Ruff、格式和差异检查通过。

## [分片回收与代码审查修复] - 2026-09-13

### 修复

- Web 服务定期回收跨 FFmpeg 重启残留的 TS、fMP4 分片、初始化文件和废弃临时文件。保护当前混流代次与播放列表引用，为旧客户端保留至少 60 秒且覆盖已观察播放列表窗口的缓冲时间；停止任务后继续回收未引用文件。播放列表无法读取或文件仍在变化时延后删除，保留无关文件和符号链接。
- 换线恢复后的手动状态检查与 OCR 测量版本快照置于同一把锁内，避免在测量启动间隙点击微调时，新手动偏移被随后返回的 OCR 结果覆盖。
- ffprobe 超时改为固定诊断，不再输出含 Cookie、Authorization 的完整命令或异常链；探测错误和 FFmpeg 日志统一脱敏请求头凭据、URL 用户信息、查询参数与片段。
- HLS 嗅探依据 TARGETDURATION / EXTINF 调整活跃窗口，兼容 4、6、10 秒分片；自动选择还需观察到后续分片或字节范围推进，避免单次下载与重复重试被误判为持续播放。

### 验证

- 新增并发手动调偏、探测超时脱敏、长分片及字节范围嗅探、播放列表与缓存保护、停止后回收及实际 FFmpeg 多次重启回归。
- 启用浏览器后的完整回归 **349 项通过、0 跳过，208.41 秒**，包含 5 项 Chromium 浏览器测试。H.264 / HEVC 各连续重启三次，回收后均保持 12 个当前代次分片，HEVC 仅保留当前初始化文件，播放列表继续正常解码。
- Ruff、格式、JavaScript 语法与差异检查通过；源码包和 wheel 无隔离构建成功，确认新的清理和脱敏模块已入包，运行代码与工作区一致。
- 本机 Python 3.14 测试进程退出时出现一次 HTTPResponse 析构警告（I/O operation on closed file），未导致测试失败。本轮使用合成媒体与 Chromium，未验收真实直播长时间运行或 Windows/macOS 原生环境。

## [开源准备与文档优化] - 2026-09-13

### 变更

- 完善 `pyproject.toml` 元数据（遵循 PEP 621 与 PEP 639），补充作者 GitHub 隐私邮箱、关键字、PyPI 分类器以及项目主页、源码与 Issue 链接。
- 优化 `README.md` 阅读体验：精简开头，以客观、实在的口吻说明“看球网站画面 + B 站解说音源”的项目初衷与实测范围；整理跨平台前置依赖一行安装表；折叠手动放包、源码编译、REST 控制接口及离线诊断说明；补充开源免责声明 (Disclaimer)。
- 移除 `CONTRIBUTING.md` 并同步清理 `MANIFEST.in` 与 README 中的指引；明确个人维护定位，暂不接收 Pull Request，欢迎通过 Issue 交流。
- 补充 `.gitignore` 规则，忽略 `/.handoff/`、离线诊断输出目录 `/ocr-debug*/` 以及常见 IDE / 操作系统配置文件。

### 验证

- `python -m build` 构建成功产出 sdist 与 wheel，检查 wheel `METADATA` 包含完整的作者、许可证表达式与项目外链。
- `ruff check src tests` 与 `ruff format --check src tests` 通过。
- 自动化脚本核对 `README.md` 本地文件链接及标题锚点，100% 验证有效。

## [OCR 首位数字与手动同步修复] - 2026-09-13

- 在验证保存框和自动候选前，检查同一基线的邻近字形并补全裁剪边界，避免 `46:15` 被裁成 `6:15` 后仍通过连续走表校验。保存框检查后续采样帧，首帧遮挡时也能恢复；继续使用源 PTS、三帧连续验证和原有识别预算。
- 本次连接中的手动偏移不再被定时 OCR 复核覆盖，复核失败也保留手动校正。主动重新同步或换线后的新时间轴测量成功后恢复自动调整；过期结果仍不能覆盖新操作。
- 增加深浅背景、四种翻转、首帧遮挡、手动延后 5 秒及恢复自动同步的回归；补充在当前偏移上增加 5 秒的操作说明。
- 非浏览器回归 **305 项通过，5 项浏览器测试未运行**；Ruff、格式与差异检查通过。留存真实素材的窄框复现和合成双路媒体的验证范围见 [验证记录](docs/validation/validation-ocr-sync-2026-09-13.md)。原直播已下播、比赛源失效，未验收现场约 5 秒的听感偏差。

## [OCR 第二轮优化与诊断反馈] - 2026-09-13

- 保存框与候选连续验证增加快检，保留后续有效三帧和停表分类；失效区域先搜索周边再回退全画面，跳过本轮已尝试的配置。成功结果仍包含完整连续样本。
- 增加适应亮度不均小字的背景校正后备样式；Tesseract 调用接收剩余预算，发现阶段预留验证时间，超时返回不能锁定，取消优先于后端超时。
- 网页展示无遮罩的选区预览、实际试读裁剪和预处理图、单帧读数、连续验证及错误状态；新设置和新源隔离旧进度，单帧或单路结果不会自行应用同步偏移。
- 新增本地 `python -m footboy.tools.ocr_diagnose` 工具，按需保存候选优先级、原始文字、图片、耗时和淘汰原因；新增图像 API 沿用控制密钥与同源限制，诊断目录自动排除版本控制。
- 与第一轮同条件各测三次：真实四帧错框恢复从 **22 次 / 1.472 秒** 降至 **10 次 / 0.663 秒**；两组四帧均完整识别、残差为零。八帧合成错框从 24 次降至 14 次，窄字与遮挡恢复保持通过；部分既有快速场景调用次数未变、耗时略升。
- 相关回归去重 **195 项通过**，包括 80 项 OCR 和 5 项浏览器测试；Ruff、格式、JavaScript 语法及差异检查通过。范围、计时口径和 RapidOCR 预算限制见 [第二轮验证记录](docs/validation/validation-ocr-round-2-2026-09-13.md)。未重启现有服务，未验收真实双路同步。

## [OCR 时钟定位优化] - 2026-09-13

- 自动定位按字符排列、数量、边缘背景和前景密度评分，再去除尺寸相近的重叠候选；候选数量上限在评分后应用，减少观众席等杂乱背景挤掉时钟的情况。
- 按候选排名交错尝试标准 / 窄字预处理、极性与四种翻转，大范围画面搜索也参与后备调度；减少无效区域的连续重复识别。
- 连续走表验证复用候选发现时的首帧读数，包括 `00:00`；保留至少三帧连续走表、源 PTS 增量、残差及停表检查。
- OCR 单元与实际 Tesseract 回归 **53 项通过，22.26 秒**。新增杂乱背景下四种翻转的小时钟识别、错误队名框恢复、首帧复用和后排候选调度回归；Ruff、格式与差异检查通过。
- 同一真实完整画面的单帧候选发现从 **165 次 / 11.083 秒**降至 **1 次 / 0.180 秒**；两组真实四帧分别从 15 秒超时、11.649 秒锁定改善为 **0.984 秒、0.579 秒**锁定，残差均为 0。新版耗时为三次中位数，不含取流采样与引擎初始化。
- 详细素材范围、计时口径与限制见 [验证记录](docs/validation/validation-ocr-2026-09-13.md)。本轮验证 Tesseract 单路时钟识别，未验收 RapidOCR 或真实双路同步精度。

## [控制面与稳定性修复] - 2026-09-12

### 修复与改进

- 控制 API、状态与采样截图统一要求本次服务启动生成的随机密钥；终端提供带片段密钥的控制链接，网页读取后清除地址栏片段，仅在当前标签页保存密钥，通过 Authorization 发送。支持手动输入、刷新恢复和过期后重新连接，密钥不写入公开页面、播放地址或状态文件。
- 控制 API 拒绝外站 Origin/Fetch Metadata 请求，修改操作要求 JSON。局域网监听与无需密钥的 HLS 播放保留，跨域响应头仅用于媒体；补充兼容 HLS Blob 媒体和 Worker 的 CSP、Referrer-Policy 与静态资源 ETag 验证缓存。
- HLS 采用有界分块读取，HEAD 只读取文件元数据，Range 仅传输请求区间；补齐空后缀、越界和超长数字范围的错误处理。
- 启动混流的耗时操作移出 Supervisor 状态锁，通过偏移与音量 revision 保留启动期间的新设置；原声音频采样接收会话取消信号，取消后不再启动 FFmpeg。Supervisor 仅在独立服务模式下创建 HTTP 服务。
- 分片健康检查按当前混流 generation 与分片序号判断进展，排除旧代、临时文件及目录，避免文件修改时间受时钟回拨影响。
- OCR 区分超时、取消、后端失败和需要框选，默认禁止隐式 GUI/终端交互；一路停表不再掩盖另一路后端错误。时间轴采样只对预期探测失败降级并输出脱敏警告，程序错误继续上报；状态文件损坏或结构无效时输出警告，加载时保留原文件。
- 增加 GitHub Actions：Python 3.10/3.14 快速检查、实际媒体/OCR、Chromium 三组任务，显式检查 FFmpeg 与 Tesseract 英文模型并启用浏览器回归。增加 slow/browser 标记，默认仍收集完整测试。

### 验证

- 全量回归 **247 项通过、0 跳过，170.59 秒**，覆盖实际 FFmpeg / ffprobe、PyAV、Tesseract，以及 1440px 和 390px Chromium 的控制密钥、刷新恢复、播放、截图、ROI、音量、偏移、换线与停止。
- 新增鉴权与跨域拒绝、HEAD/Range 有界读取、启动期间手动操作与取消、时钟回拨、OCR 错误分类和损坏状态文件回归。后续补充非法密钥输入处理；浏览器等待改为测试进程轮询，避免等待脚本的异步 eval 与 CSP 冲突。
- 最终 HTTP 回归通过；3 项 Chromium 流程复核通过（101.47 秒），覆盖非法密钥输入及 CSP 下的完整播放和换线。Ruff、格式、JavaScript 语法、差异检查及源码包/wheel 无隔离构建通过。

### 已知边界

- 保留可信局域网 HTTP 使用方式；HLS 播放无需密钥，媒体客户端仍允许访问本机与内网源，没有实现覆盖 DNS、重定向和所有媒体子请求的出站网络隔离。
- 本机验证使用 Python 3.14 和合成媒体，不代表真实双路同步精度、长期运行、Windows/macOS 原生或实体 Safari 验收。CI 工作流尚未在 GitHub 执行。

## [文档优化与界面截图] - 2026-09-11

- README 增加项目徽章、高分辨率 WebUI 真实控制台截图（`docs/images/webui-1440.png`）、Mermaid 纵向数据流架构图与目录索引（TOC）。
- 优化快速开始指引，简化默认启动命令并强调 `--ocr auto` 自动侦测，补充常规安装与开发安装说明，提前提示 Linux Playwright 系统依赖安装选项（`--with-deps`）。
- 新增「跨设备与局域网观看」章节，提供 iOS Safari 原生硬解、智能电视/盒子与 VLC/IINA/PotPlayer 串流播放指引，补充各系统（Windows/Linux/macOS）防火墙放行命令参考及播放延迟说明。
- 补充日常手动调偏白话直觉对照，补齐 CLI 参数表中遗漏的 `--headless-sniff`、`--ocr`、`--tesseract-command`，并单列 `--offset` 与 `footboy-p0` 调用示例。

## [外部工具查找] - 2026-09-11

- FFmpeg、ffprobe、Tesseract 支持自定义程序路径；默认先查 PATH，再查当前目录和源码项目的 `tools`。环境检查、媒体探测、混流与 OCR 共用查找逻辑。
- README 补充工具目录结构、各平台安装方式和自定义路径示例；Git 忽略本地 `tools`。
- 完整回归 190 项通过、3 项浏览器测试未启用；随后补充调用路径验证，环境相关测试共 25 项通过。Ruff、格式与差异检查通过。未进行 Windows/macOS 原生安装验收。

## [开源准备] - 2026-09-11

### 变更

- 统一 README、网页标题与品牌、命令行帮助和包描述中的名称为 Footboy（足小子）。命令与 Python 包名继续使用 `footboy`。
- 项目代码采用 MIT，新增根目录 LICENSE；保留 hls.js 1.6.13 的原始版权声明，补齐 Apache-2.0 全文及 THIRD_PARTY_NOTICES.md，记录来源和校验值。
- 发行包使用 `MIT AND Apache-2.0` 许可表达式，纳入项目和第三方许可文件；提高 setuptools 构建下限以支持这些标准元数据。
- 整理 README 的快速开始、连接观看、同步原理、CLI/API、故障排查及验证范围；补充项目目录、平台命令、OCR 前提、Node.js 检查依赖，并改用不依赖环境预装构建后端的隔离构建命令。
- 保留脱敏的历史验证文档，修正文句；新增 CONTRIBUTING.md 公开维护提交、验证、源 PTS 与隐私约定。补充全小写 agent 文件的忽略和源码包排除规则，本地协作文件继续保留在工作区。
- 明确控制 API 无身份认证、允许跨域请求，诊断信息可能保留来源域名与路径；软件许可不授予直播内容的使用或传播权。

### 验证

- 复核变更前全部 8 次提交、`v0.1.0` 附注标签、分支与 reflog，以及全部 199 个 Git 对象（130 个文件对象）。未发现真实直播源标识、真实凭据、私钥、媒体或被禁止跟踪的协作文件；凭据与签名参数命中均为测试占位值。提交及标签使用 GitHub 隐私邮箱，`git fsck --full --no-reflogs --unreachable` 通过且无不可达对象。
- hls.js 及其简短许可声明分别与上游 1.6.13 的 npm 发布文件和源码标签逐字节一致；上游源码树没有单独的 NOTICE 文件。
- 完整回归 **185 项通过，0 跳过，153.39 秒**，包含本地合成媒体、实际 Tesseract、1440px 与 390px 桌面 Chromium 流程。Ruff、格式、JavaScript 语法、文档相对链接、命令行选项及差异检查通过。
- 在不含运行数据的临时源码副本和全新 Python 3.14 虚拟环境中，从 PyPI 安装 `.[dev,tesseract]`、执行 `pip check` 与 `python -m build` 成功；额外的实际 Tesseract 合成时钟识别检查通过。
- 源码包和 wheel 的许可元数据、四份许可相关文件及全部播放器资源核对通过；未包含 agent 文件、工具配置、状态、Cookie、媒体或缓存。将 wheel 安装进上述环境后，从 site-packages 启动 CLI 和临时控制服务，验证静态资源、两种视口的品牌显示、无横向溢出与脚本异常，以及 Ctrl+C 正常退出。

### 已知限制

- 本轮没有读取真实直播源；回归不代表真实双路同步、长期运行、Windows/macOS 原生或实体手机 Safari 验收。新虚拟环境复用本机已安装的 FFmpeg、Tesseract 及匹配版本的 Chromium，未重新安装这些系统组件；RapidOCR 未验收。
- 历史审查范围为当前本地仓库，尚未配置远端；不覆盖其他副本或对源码权属的独立证明。旧提交、标签和旧构建产物未改写，首次公开发行应基于补齐许可后的版本重新构建。
- 本轮补充了部署边界说明，未增加控制 API 的认证或跨域访问限制；服务仍只适合可信的本机或局域网使用。

## [审查修复] - 2026-09-11

### 修复

- 启动阶段发生手动偏移调整后跳过自动初测，保留当前值及保存值，并继续安排后续周期复核。检查手动状态和启动测量使用同一把锁，避免期间的新手调被当成自动测量的基准版本。
- H.264 输出追加旧 HLS 列表前检查现有封装；已有 fMP4 初始化映射或非 MPEG-TS 分片时生成新列表，修复 HEVC 转 H.264 后旧 `.m4s` 丢失 `EXT-X-MAP` 导致无法解析的问题。同一混流器重启和新建混流器均适用，旧初始化文件与分片继续保留供已有播放器读取。
- 保持源 PTS 对齐、视频复制输出、正常自动初测及已有过期测量保护。

### 验证

- 新增 6 项回归，修复前确认 3 个启动时机的手动偏移覆盖及 2 个编码切换场景失败；无手调时的自动初测正常。修复后新增回归全部通过，检查手动值持久化、周期复核调度与待应用状态。
- 相关回归合计 **63 项通过，0 跳过**：控制、音量与混流单元回归 37 项；完整媒体集成 26 项，耗时 125.88 秒，包含桌面 1440px、手机尺寸 390px Chromium 的播放、偏移、ROI、混音、换线及停止流程。
- 编码切换使用本地合成 HEVC/H.264 媒体和实际 FFmpeg / ffprobe、PyAV；以短于播放列表窗口的新流检查输出可解析、视频像素一致、相对 PTS 保持、DTS 异常为 0，且旧初始化文件及分片内容不变。
- Ruff、格式、JavaScript 语法、FFmpeg / ffprobe 环境及差异检查通过。

### 已知限制

- 启动竞争回归使用模拟 OCR 结果；媒体及浏览器回归使用本地合成源，不代表真实直播同步精度验收。
- 格式切换仍需等待新分片与播放器缓冲。本轮未进行真实直播、Windows/macOS 原生或实体手机 Safari 验收；手机尺寸 Chromium 不等同于实体手机测试。

## [仓库维护] - 2026-09-10

### 变更

- 重写既有 6 次提交及 `v0.1.0` 标签，清理 README、验证记录、更新日志和页面示例中的真实直播链接、房间号及可识别的直播间描述。测试使用无效房间号与模拟域名，并同步对应的状态键。
- 从全部历史树移除本地协作文件；所有历史版本的 `.gitignore` 均在任意目录忽略 `AGENT.md`、`AGENTS.md`、`.codex`、`.agents` 和 `.claude`，源码打包清单也排除这些文件与目录。本地协作文件保留供工作区使用。
- 删除历史重写产生的旧引用，清空旧 reflog 并回收不可达对象；保留原提交作者、时间、说明和标签注释。

### 验证

- 遍历全部分支、标签及 Git 对象，未发现被禁止跟踪的路径、已识别的真实直播标识或遗留不可达对象；`git fsck --full` 通过。核对所有历史 Python 文件语法，运行逻辑文件与清理前一致。
- 完整回归 **179 项通过，0 跳过，152.07 秒**，使用本地合成媒体，包含 FFmpeg、实际 Tesseract 和桌面及手机尺寸 Chromium 流程。Ruff、格式、JavaScript 语法、FFmpeg / ffprobe 环境与差异检查通过。
- 源码包和 wheel 构建成功；核对文件清单，不包含协作文件、工具配置、状态和本地测试产物，播放器静态资源及许可齐全。

### 已知限制

- 仓库未配置远端，本次清理范围为当前本地仓库；其他独立副本不随本次历史重写更新。
- 本轮没有进行真实直播或 Windows/macOS 原生、实体手机 Safari 验收。

## [未发布] - 2026-09-10

### 功能与修复

- 增加独立的“原直播音量”“B 站解说音量”滑块及原声开关；支持各自 0–100%、单路静音和 `/api/audio` 部分字段更新。设置作用于 HLS，连续调整合并应用，换线及手动同步保留音量；新线路无音轨时关闭原声。
- 混音前统一采样率，再按共同源 PTS 起点补静音或裁剪，以独立增益相加并限幅；避免 amix 按样本直接叠加破坏偏移，以及 44.1kHz/48kHz 混用时起点换算错误造成无声。仅混音或音量处理转 AAC，默认 B 站 AAC 仍复制，视频始终复制。
- “立即同步”重新 OCR 并应用 `D = K_B - K_V`；成功时即使偏移未变也刷新混流及播放器。保留手动优先与过期结果保护，音量调整不会使 OCR 结果过期或被 OCR 覆盖。
- 首次无保存或指定偏移时并行采样源 PTS，估算起始时间轴，明确保持“未对齐”且不持久化估算值。修复亮色记分牌内大号深色时钟的轮廓定位。

### 验证

- 完整回归 **179 项通过，0 跳过，150.13 秒**，含桌面 1440px、手机尺寸 390px Chromium 的独立音量操作、播放、偏移、ROI、换线和停止流程。
- 合成源实际 FFmpeg 验证两路独立增益及单路静音：44.1kHz/48kHz 原声与 48kHz 解说的 700Hz、1200Hz 频谱分别核对 100%、25%、0%，视频像素一致；另验证解说推迟 2 秒时不会提前混入，以及相差 4719 秒的源 PTS 起点估算。Ruff、格式、JavaScript 语法及差异检查通过。
- 当前真实双路直播在新版预览中开启原直播 25%、B 站 100%；修复采样率换算后抽检分片含 50 个视频包和 94 个音频包，输出为 48kHz 双声道。真实直播在 1440px、390px Chromium 持续播放检查通过，页面两路音量值独立且无横向溢出。
- 真实 B 站截图的自动定位读出 47:08；当前直播使用保存区域完成两路连续走表，状态记录视频 5 帧、B 站 4 帧，最大残差 0.400 秒。单帧自动定位结果不作为连续走表验收。

### 已知限制

- 原声包含原直播自带解说，不能分离为纯球场环境声。混音音量只保留在本次任务内，应用时需重新缓冲；总音量过高时限幅保护输出。
- 初始时间轴估算受源延迟和请求耗时影响，不能替代比赛内容同步；保存或手动指定的偏移优先。OCR 仍需至少 3 帧连续走表，中场或无计时画面不能保证识别。
- 真实直播仅做短时取流、分片及浏览器检查；尚未验收长期运行、精确双路同步误差、Windows/macOS 原生或实体手机 Safari。

## [0.1.4] - 2026-09-09

### 功能与修复

- 增加 WebUI 按名称选择及切换比赛线路、CLI `--video-line` 和 `/api/line`；支持 iframe、动态线路列表、圈号与全角数字。明确指定的名称未找到时等待重新选择，“重新选线路”入口等待人工选择。
- 新源通过探测后替换原画面；换线失败保留原源，自动模式重新确认源 PTS，旧源 OCR 结果及切换期间的过期测量不能覆盖新的手动设置。
- 处理 HTTPS 播放器请求 HTTP 媒体而被浏览器拦截的情况：读取已观察到的请求，拒绝结束或点播播放列表，保留请求上下文并通过 ffprobe 验收。页面 HTTP 错误直接报告访问失败。
- 修复足球计时冒号两侧间距较大时自动定位切开数字的问题，保留细长小计时的原有识别方式及连续走表校验。
- 补齐 libavformat 对显式端口的 Cookie 域名匹配，保留普通域名及路径约束；视频复制输出与 `D = K_B - K_V` 不变。
- 清理 README 和历史验证记录中的真实直播链接，改用匿名描述及本机环境变量；更新版本和使用说明。

### 验证

- 真实足球页面“高清直播⑤”经正式嗅探约 22.1 秒确认 HLS，实际 ffprobe 验收为 1920×1080 H.264 / AAC。真实关键帧自动 OCR 连续读出 `81:56 → 82:00 → 82:04 → 82:08`，源 PTS 每次前进 4 秒，4 帧残差为 0；同批样本识别耗时 5.421 秒，不含网络采样。
- 本地合成媒体的手机尺寸 Chromium 流程通过 iframe 线路选择、运行中切换、等待期间继续播放、+0.5 秒手动偏移保留、HLS 恢复与停止；Cookie 上下文经实际 ffprobe、FFmpeg、PyAV 读取验证。
- 完整回归 **154 项通过，0 跳过，124.96 秒**；Ruff、格式、JavaScript 语法和差异检查通过。0.1.4 源码包与 wheel 构建成功，核对新模块、静态资源、许可和版本，并检查没有纳入私有输入或本地测试产物。
- 条件、实际读数及限制见 [0.1.4 验证记录](docs/validation/validation-0.1.4.md)。

### 已知限制

- 后续提供的另一比赛页面在当前环境返回 Cloudflare HTTP 403，未取得线路或完成 OCR；第一场结束后的修复对比使用已采集真实帧。
- 真实部分只验收单路足球取流和 OCR，切流播放流程使用合成媒体。未验收不同来源同场足球的双路同步精度、长期运行、RapidOCR、Windows/macOS 原生或实体手机 Safari。

## [0.1.3] - 2026-09-09

### 修复

- 修复真实直播顶部中央小计时框无法自动定位的问题：搜索紧凑短文本行，避开相邻比分和经济数值，并补充顶部、底部中央搜索区域。
- 为细长亮色数字增加备用预处理和 Tesseract 原始单行识别；优先在已保存或手选 ROI 中重试，保存识别样式并兼容旧状态文件。保持源 PTS、连续走表校验、手动优先及过期结果保护。
- 补充小计时、倒计时干扰、翻转和配置兼容回归，更新版本与使用说明。

### 验证

- 视频样本 A 真实非足球赛事重播的两批 7/6 个关键帧，经实际 Tesseract 自动锁定 `04:47 → 04:50 → 04:53` 和 `24:42 → 24:45 → 24:48`，源 PTS 每次前进 3 秒，所选连续样本残差为 0。
- 同一房间 H.264 媒体的独立双连接在网页自动测得 D=0.000s；赛间无时钟时保留原偏移，重测期间 +0.5 秒手调不被旧结果覆盖。最终约 78 秒的完整流程通过两路快照、保存区域重测、偏移应用与停止，DTS 和页面脚本异常均为 0。
- 完整 128 项回归通过、0 跳过，耗时 77.69 秒；Ruff、格式、JavaScript 语法及差异检查通过。0.1.3 源码包与 wheel 构建成功。条件与样本含义见 [0.1.3 验证记录](docs/validation/validation-0.1.3.md)。

### 已知限制

- 单帧仍有漏读、误读，手选区域复测曾出现 1 秒误差；成功锁定依赖连续样本和残差门限，不代表逐帧全部正确。
- 本轮为单一房间赛事重播，双路使用同一媒体独立连接；不代表不同来源的同场足球同步精度验收。RapidOCR、Windows/macOS 原生、实体手机 Safari、长延迟缓冲与两小时连续运行仍未验收。

## [0.1.2] - 2026-09-09

### 修复

- 修复新版 Chromium 声明支持原生 HLS 后，控制页绕过 hls.js、在真实 B 站混流分片上解析失败并停播的问题。支持 MSE 的非 Apple 浏览器优先使用包内 hls.js，Apple 浏览器保留原生播放路径。
- 浏览器回归覆盖声明原生 HLS 能力的 Chromium，验证实际播放时间前进、偏移后恢复及本地 Blob Worker；更新版本号、使用说明和实播记录。

### 验证

- B 站视频样本 A 与音频样本 B 实播通过约 85 秒和 182 秒的功能流程，全程关闭自动 OCR。验证六档微调、复制地址、媒体 Range、普通退出恢复、恢复期间手动优先、停止与保存偏移后重连。
- 同期输入输出匹配 6 个像素相同的视频关键帧、675 个相同 AAC 包；源 PTS 相对位移误差约 0.167ms，DTS 异常为 0。偏移调整后约 4.2–6.2 秒恢复，主动普通退出后约 6.1 秒恢复。
- 排除真实 OCR 集成后的 109 项回归全部通过、0 跳过，耗时 67.21 秒；Ruff、JavaScript 语法和差异检查通过。源码包与 wheel 构建成功，核对播放器代码、hls.js、许可和版本完整性。
- 详细条件与测量含义见 [0.1.2 验证记录](docs/validation/validation-0.1.2.md)。

### 已知限制

- 两个房间内容不同，视频样本 A 当时播放赛事重播；本轮不验收同场比赛内容同步精度或 OCR。不同编码/线路 PTS 起点不同，切换后须重新确认偏移。
- 真实 B 站页面遇到专题跳转和验证码，页面嗅探仍待实播验收；视频直链及 B 站房间解析已通过。
- 本机 Chromium 不支持 HEVC，实播视频使用现有 H.264 线路并保持复制输出。未完成实体手机 Safari、Windows/macOS 原生、签名自然过期、实际长延迟缓冲及两小时连续运行验收。

## [0.1.1] - 2026-09-09

### 修复与功能

- 补齐“比赛源直连”设置：WebUI、CLI 和 P0 支持，覆盖页面嗅探、ffprobe、PyAV 采样及 FFmpeg 混流；B 站保留独立的代理访问。
- 修复空代理选项在 HLS 子请求中丢失的问题，使分片和 AES-128 密钥请求也遵守直连设置，保留请求上下文、源 PTS 和视频复制输出。
- 修复环境检查误拒绝新版 `N-…` FFmpeg 构建的问题，按运行时 libavformat 确认 FFmpeg 6 的兼容下限。
- FFmpeg 原生崩溃后停止自动重取源，显示更换构建的诊断并保留控制台重新连接能力；普通取流失败仍可恢复，旧测量不能覆盖手动偏移。ffprobe 崩溃也给出明确诊断。

### 验证

- 118 项完整回归通过，0 跳过：98 项基础/HTTP、9 项 FFmpeg 媒体、9 项真实 Tesseract、2 项浏览器流程；耗时 68.63 秒。
- 本地代理验证普通和 AES-128 HLS 的探测、采样、混流、分片及密钥直连，B 站请求继续经过代理；源 PTS 及视频像素保持验证通过。
- 同一本地合成 HLS 在此前使用的 7.0.2 静态构建上复现 SIGSEGV，在新构建上正常读取；新构建的 CLI 环境检查、Ruff 及 JavaScript 语法检查通过。
- 本轮环境与范围见 [0.1.1 验证记录](docs/validation/validation-0.1.1.md)。

### 已知限制

- 此前两个实播测试地址已失效，本轮使用本地合成源，未完成新的实播验收。
- 真实比赛连续 2 小时、实际长延迟缓冲、实体手机 Safari 和 Windows/macOS 原生运行仍待验证；模拟 Windows 异常退出码不代表实机验收。

## [0.1.0] - 2026-09-09

### 功能

- 建立 P0/P1/P2 可运行基线：B 站 FLV 解析、比赛页面浏览器嗅探、源 PTS 对齐、视频复制混流和局域网 HLS 服务。
- WebUI 支持输入两路地址、直链及请求头、启停任务、线路选择、HLS 预览、六档偏移微调、两路截图翻转和时钟区域框选。
- OCR 支持 RapidOCR/Tesseract、已保存区域复用、自动发现、连续走表校验、停表拒绝、周期复核和连续两次漂移确认。
- 支持断流恢复、B 站重解析、第三方重嗅探、状态持久化与独立的 P0 手工验收命令。
- hls.js 与许可文件随包提供，控制页无需外部 CDN；HEVC 使用 fMP4 和独立初始化文件。

### 稳定性

- 控制页在取流前可用；OCR 在后台运行，新手动设置和新源时间轴不会被旧测量覆盖。
- WebUI 继承命令行的手动模式、初始偏移和直链设置；停止请求不会在嗅探启动时被清除。
- 嗅探依据播放列表的实际分片请求判断活跃线路，探测失败后可继续选线，取流请求保留所需请求上下文。
- HLS 重启使用独立分片名称，保留 discontinuity，避免覆盖旧分片和 HEVC 初始化文件；HTTP 支持媒体 Range 请求。
- 修正合成直播夹具中时间戳为 0 的 FLV 编码头被误作媒体起点的问题。

### 验证与版本管理

- 完整回归 90 项通过，0 跳过：72 项基础/HTTP 检查、7 项 FFmpeg 媒体集成、9 项真实 Tesseract 检查、2 项浏览器流程验证。
- 1440px 桌面与 390px 手机尺寸通过播放、框选、手动偏移及停止流程；本地加速合成源中调整后约 4.1 秒恢复播放。
- Ruff、JavaScript 语法检查通过；生成源码包及 wheel，并核对运行代码、WebUI、hls.js 与许可文件完整性。
- 新增 Git 忽略规则、协作约定和提交前检查钩子；每次提交必须同时更新并暂存本日志。
- 详细环境与验收范围见 [验证记录](docs/validation/validation.md)。

### 已知限制

- 尚未完成真实比赛连续 2 小时、真实长延迟双路缓冲及实体手机 Safari 连播 30 分钟的验收。
- Windows/macOS 尚未实机验证；HEVC 播放取决于浏览器解码能力。
- 音频 GCC-PHAT 模块尚未接入控制流程，时钟跳变处逐帧精细化留待后续阶段。
